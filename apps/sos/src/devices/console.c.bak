


int dev_console_create () {
	/* devname, devops, blockdev?, opts */

	/* need: underlying block count
						block size
						data?
 	*/
	return vfs_register_special ("console", operations, NULL, NULL);
}

static const vfs_optable_t operations = {
	serial_open,
	serial_close,

	serial_write,
	serial_read,

	NULL,	
};